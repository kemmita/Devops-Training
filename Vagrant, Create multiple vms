1. open up gitbash and run vagrant init in a place of your choosing
2. use nano to gain access to the Vagrantfile "nano Vagrantfile"
3. inside of the vagrant file add these two lines of code below
    # Every Vagrant development environment requires a box. You can search for
    # boxes at https://vagrantcloud.com/search.
    config.vm.box = "ubuntu/trusty64"
    config.vm.network "private_network", type: "dhcp"
4. save the file and run vagrant up
5. Then run vagrant ssh
6. once in the vm run hostname -I to obtian the ip address
7. open putty and use the ip we retrieved, to login use user: vagrant and password: vagrant
8. done
