create a new js or ts file 


import { Selector } from 'testcafe';

fixture `Getting Started`
    //below we direct cafe where to go to
   .page `http://carpedata.thecompletewebhosting.com/`;
        test('4 seasons test', async t => {
                await t
                //the first two below will test input fields, this is done by using the html element id/class, the last one will be
                //the buttton that is identified also by its id. 
                .typeText('#name', 'Russell Kemmit')
                .typeText('#email', 'kemmitr@yahoo.com')
                .click('#b6');
        });
